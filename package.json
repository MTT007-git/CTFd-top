{
  "name": "ctfd-top-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Browser extension that shows which top CTFd leaderboard players solved each challenge.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "node build.mjs && node test/run.mjs",
    "clean": "node build.mjs --clean"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.21.5",
    "typescript": "^5.5.4"
  }
}
